node {

    stage 'Checkout'
        checkout scm
    stage 'Build'
        sh "docker build -t test:B${BUILD_NUMBER} -f dockerfile ."
    stage 'Test'
        sh "docker run --rm test:B${BUILD_NUMBER}"
}
